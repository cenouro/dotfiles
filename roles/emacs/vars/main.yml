---
emacs_d: '{{ HOME }}/.emacs.d'

init_el:
  src: '{{ PWD }}/init.el'
  dest: '{{ emacs_d }}/init.el'

elisp:
  src: '{{ PWD }}/elisp'
  dest: '{{ emacs_d }}/elisp'

templates:
  src: '{{ PWD }}/templates'
  dest: '{{ emacs_d }}/templates'

custom_el: '{{ emacs_d }}/custom.el'
...
